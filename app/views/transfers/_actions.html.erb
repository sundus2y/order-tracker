<div class="dropdown">
  <button type="button" class="dropbtn">Actions <i class="fa fa-chevron-down"></i></button>
  <div id="myDropdown" class="dropdown-content myDropdown">
    <%= button_tag('Save', type: 'submit') if policy(@transfer).edit? %>
    <%= button_tag('Transfer', type: 'submit', data:{action: submit_transfer_path(@transfer)}) if policy(@transfer).transfer? %>
  </div>
</div>