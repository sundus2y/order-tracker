<% content_for :modal_header, flush: true do %>
    View Customer
<% end %>

<% content_for :modal_body, flush: true do %>
    <%= render 'form', action: "Show", remote: true, viewOnlyMode: true %>
<% end %>

<%= render "/modals/base", id: 'pop_up' %>

<script>
    (function () {
        $(document).ready(function(){
            var App = CustomerCarApp.App;
            ReactDOM.render(
                React.createElement(App, {viewOnly: true}, null),
                $('.customer_cars_show_app')[0]
            );
        });
    })();
</script>